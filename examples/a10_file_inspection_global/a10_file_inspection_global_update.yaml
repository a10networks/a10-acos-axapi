
- name: Update a10_file_inspection_global example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_inspection_global instance
    a10_file_inspection_global:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
