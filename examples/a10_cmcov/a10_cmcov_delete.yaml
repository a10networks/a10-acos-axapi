- name: Delete a10_cmcov example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cmcov instance
    a10_cmcov:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


