- name: Delete a10_snmp_server_enable_traps_routing_isis example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_snmp_server_enable_traps_routing_isis instance
    a10_snmp_server_enable_traps_routing_isis:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


















