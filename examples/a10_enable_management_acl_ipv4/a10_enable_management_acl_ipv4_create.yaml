
- name: Create a10_enable_management_acl_ipv4 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_enable_management_acl_ipv4 instance
    a10_enable_management_acl_ipv4:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
