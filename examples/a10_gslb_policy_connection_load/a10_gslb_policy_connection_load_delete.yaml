- name: Delete a10_gslb_policy_connection_load example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_policy_connection_load instance
    a10_gslb_policy_connection_load:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent







