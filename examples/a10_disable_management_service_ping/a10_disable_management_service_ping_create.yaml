
- name: Create a10_disable_management_service_ping example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_disable_management_service_ping instance
    a10_disable_management_service_ping:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
