- name: Delete a10_vrrp_a_state_cpu example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_vrrp_a_state_cpu instance
    a10_vrrp_a_state_cpu:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

