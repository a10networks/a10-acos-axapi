- name: Delete a10_chassis_info example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_chassis_info instance
    a10_chassis_info:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

