- name: Delete a10_dnssec_key_rollover example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_dnssec_key_rollover instance
    a10_dnssec_key_rollover:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent





