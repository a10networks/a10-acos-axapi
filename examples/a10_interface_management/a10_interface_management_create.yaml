
- name: Create a10_interface_management example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_interface_management instance
    a10_interface_management:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
