
- name: Create a10_delete_web_category example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_delete_web_category instance
    a10_delete_web_category:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
