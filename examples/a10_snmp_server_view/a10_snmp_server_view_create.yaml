
- name: Create a10_snmp_server_view example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_view instance
    a10_snmp_server_view:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      oid: NO_EXAMPLE
      viewname: NO_EXAMPLE
