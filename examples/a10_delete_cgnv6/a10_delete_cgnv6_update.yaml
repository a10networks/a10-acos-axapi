
- name: Update a10_delete_cgnv6 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_delete_cgnv6 instance
    a10_delete_cgnv6:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
