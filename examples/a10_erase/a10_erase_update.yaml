
- name: Update a10_erase example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_erase instance
    a10_erase:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
