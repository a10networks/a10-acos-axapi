
- name: Create a10_admin_session example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_admin_session instance
    a10_admin_session:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
