
- name: Update a10_file_local_uri_file example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_local_uri_file instance
    a10_file_local_uri_file:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
