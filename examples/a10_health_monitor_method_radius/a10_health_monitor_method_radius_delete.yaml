- name: Delete a10_health_monitor_method_radius example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_health_monitor_method_radius instance
    a10_health_monitor_method_radius:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent










