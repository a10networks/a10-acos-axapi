
- name: Update a10_snmp_server_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_snmp_server_group instance
    a10_snmp_server_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      groupname: NO_EXAMPLE
