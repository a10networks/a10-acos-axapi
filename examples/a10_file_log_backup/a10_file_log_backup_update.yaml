
- name: Update a10_file_log_backup example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_log_backup instance
    a10_file_log_backup:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
