
- name: Create a10_gslb_policy_auto_map example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_policy_auto_map instance
    a10_gslb_policy_auto_map:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
