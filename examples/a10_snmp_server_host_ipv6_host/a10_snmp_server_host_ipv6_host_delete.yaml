- name: Delete a10_snmp_server_host_ipv6_host example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_snmp_server_host_ipv6_host instance
    a10_snmp_server_host_ipv6_host:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

      ipv6_addr: NO_EXAMPLE




      version: v1

