
- name: Create a10_sys_ut_event_action_tcp_flags example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_sys_ut_event_action_tcp_flags instance
    a10_sys_ut_event_action_tcp_flags:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
