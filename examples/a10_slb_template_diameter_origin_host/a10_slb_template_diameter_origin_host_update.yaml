
- name: Update a10_slb_template_diameter_origin_host example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_slb_template_diameter_origin_host instance
    a10_slb_template_diameter_origin_host:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
