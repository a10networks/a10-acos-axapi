
- name: Create a10_debug_ospf example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_ospf instance
    a10_debug_ospf:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
