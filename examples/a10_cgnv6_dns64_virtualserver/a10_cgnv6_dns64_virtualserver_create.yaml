
- name: Create a10_cgnv6_dns64_virtualserver example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_dns64_virtualserver instance
    a10_cgnv6_dns64_virtualserver:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_dns64-virtualserver
