
- name: Create a10_debug_rip example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_rip instance
    a10_debug_rip:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
