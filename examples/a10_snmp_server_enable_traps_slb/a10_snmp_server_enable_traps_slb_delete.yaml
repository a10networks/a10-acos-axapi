- name: Delete a10_snmp_server_enable_traps_slb example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_snmp_server_enable_traps_slb instance
    a10_snmp_server_enable_traps_slb:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent





























