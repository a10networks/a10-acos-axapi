
- name: Create a10_accounting example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_accounting instance
    a10_accounting:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
