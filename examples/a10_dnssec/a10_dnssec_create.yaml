
- name: Create a10_dnssec example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_dnssec instance
    a10_dnssec:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
