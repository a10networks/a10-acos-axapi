
- name: Create a10_router_ipv6_ospf example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_router_ipv6_ospf instance
    a10_router_ipv6_ospf:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      process_id: NO_EXAMPLE
