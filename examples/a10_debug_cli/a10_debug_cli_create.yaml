
- name: Create a10_debug_cli example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_cli instance
    a10_debug_cli:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
