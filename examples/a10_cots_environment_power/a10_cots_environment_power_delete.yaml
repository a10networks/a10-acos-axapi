- name: Delete a10_cots_environment_power example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cots_environment_power instance
    a10_cots_environment_power:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

