
- name: Update a10_snmp_server_SNMPv3_user example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_snmp_server_SNMPv3_user instance
    a10_snmp_server_SNMPv3_user:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      username: NO_EXAMPLE
