- name: Delete a10_acos_events_message_selector example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_acos_events_message_selector instance
    a10_acos_events_message_selector:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

      name: my_message-selector



