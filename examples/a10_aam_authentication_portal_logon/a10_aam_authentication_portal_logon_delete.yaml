- name: Delete a10_aam_authentication_portal_logon example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_aam_authentication_portal_logon instance
    a10_aam_authentication_portal_logon:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent












