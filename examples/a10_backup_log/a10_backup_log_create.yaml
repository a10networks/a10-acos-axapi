
- name: Create a10_backup_log example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_backup_log instance
    a10_backup_log:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
