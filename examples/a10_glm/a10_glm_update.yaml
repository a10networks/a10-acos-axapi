
- name: Update a10_glm example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_glm instance
    a10_glm:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
