- name: Delete a10_glm_proxy_server example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_glm_proxy_server instance
    a10_glm_proxy_server:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent







