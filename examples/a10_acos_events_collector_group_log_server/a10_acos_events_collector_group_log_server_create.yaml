
- name: Create a10_acos_events_collector_group_log_server example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_acos_events_collector_group_log_server instance
    a10_acos_events_collector_group_log_server:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_log-server
