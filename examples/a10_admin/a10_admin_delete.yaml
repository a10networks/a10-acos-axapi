- name: Delete a10_admin example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_admin instance
    a10_admin:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




      user: NO_EXAMPLE












