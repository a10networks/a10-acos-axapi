
- name: Update a10_aam_authentication_logon_http_authenticate_instance example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_aam_authentication_logon_http_authenticate_instance instance
    a10_aam_authentication_logon_http_authenticate_instance:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      name: my_instance
