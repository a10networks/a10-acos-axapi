
- name: Update a10_cgnv6_stateful_firewall_endpoint_independent_filtering_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_stateful_firewall_endpoint_independent_filtering_tcp instance
    a10_cgnv6_stateful_firewall_endpoint_independent_filtering_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
