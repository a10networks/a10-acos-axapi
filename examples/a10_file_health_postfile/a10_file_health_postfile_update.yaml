
- name: Update a10_file_health_postfile example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_health_postfile instance
    a10_file_health_postfile:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
