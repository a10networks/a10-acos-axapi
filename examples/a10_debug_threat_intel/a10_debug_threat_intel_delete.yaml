- name: Delete a10_debug_threat_intel example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_debug_threat_intel instance
    a10_debug_threat_intel:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

