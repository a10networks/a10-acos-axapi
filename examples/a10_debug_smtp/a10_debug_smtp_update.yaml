
- name: Update a10_debug_smtp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_smtp instance
    a10_debug_smtp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
