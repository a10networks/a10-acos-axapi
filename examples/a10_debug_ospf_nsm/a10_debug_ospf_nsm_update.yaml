
- name: Update a10_debug_ospf_nsm example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_ospf_nsm instance
    a10_debug_ospf_nsm:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
