
- name: Create a10_accounting_exec example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_accounting_exec instance
    a10_accounting_exec:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
