
- name: Create a10_scaleout_cluster_local_device example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_scaleout_cluster_local_device instance
    a10_scaleout_cluster_local_device:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
