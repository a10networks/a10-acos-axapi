
- name: Update a10_gslb_policy_capacity example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_policy_capacity instance
    a10_gslb_policy_capacity:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
