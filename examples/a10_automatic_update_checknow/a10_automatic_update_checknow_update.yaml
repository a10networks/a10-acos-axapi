
- name: Update a10_automatic_update_checknow example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_automatic_update_checknow instance
    a10_automatic_update_checknow:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
