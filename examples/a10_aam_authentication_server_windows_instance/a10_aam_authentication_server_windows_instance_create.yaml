
- name: Create a10_aam_authentication_server_windows_instance example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_aam_authentication_server_windows_instance instance
    a10_aam_authentication_server_windows_instance:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_instance
