
- name: Update a10_bootimage example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_bootimage instance
    a10_bootimage:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
