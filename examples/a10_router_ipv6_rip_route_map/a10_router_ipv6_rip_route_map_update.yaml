
- name: Update a10_router_ipv6_rip_route_map example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_router_ipv6_rip_route_map instance
    a10_router_ipv6_rip_route_map:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
