
- name: Create a10_axdebug example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_axdebug instance
    a10_axdebug:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
