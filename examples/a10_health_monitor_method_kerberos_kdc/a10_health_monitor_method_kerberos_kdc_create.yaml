
- name: Create a10_health_monitor_method_kerberos_kdc example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_health_monitor_method_kerberos_kdc instance
    a10_health_monitor_method_kerberos_kdc:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
