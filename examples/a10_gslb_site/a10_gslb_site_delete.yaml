- name: Delete a10_gslb_site example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_site instance
    a10_gslb_site:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



      site_name: NO_EXAMPLE














