
- name: Update a10_debug_http_proxy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_http_proxy instance
    a10_debug_http_proxy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
