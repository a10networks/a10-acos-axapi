
- name: Update a10_cgnv6_nat64_prefix example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_nat64_prefix instance
    a10_cgnv6_nat64_prefix:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      prefix_val: NO_EXAMPLE
