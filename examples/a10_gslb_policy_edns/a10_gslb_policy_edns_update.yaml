
- name: Update a10_gslb_policy_edns example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_policy_edns instance
    a10_gslb_policy_edns:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
