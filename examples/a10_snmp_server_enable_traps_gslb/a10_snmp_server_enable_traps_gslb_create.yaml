
- name: Create a10_snmp_server_enable_traps_gslb example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_enable_traps_gslb instance
    a10_snmp_server_enable_traps_gslb:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
