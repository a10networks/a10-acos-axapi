
- name: Create a10_cgnv6_server example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_server instance
    a10_cgnv6_server:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_server
