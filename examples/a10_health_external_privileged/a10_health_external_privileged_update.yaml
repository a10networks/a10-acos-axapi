
- name: Update a10_health_external_privileged example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_health_external_privileged instance
    a10_health_external_privileged:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
