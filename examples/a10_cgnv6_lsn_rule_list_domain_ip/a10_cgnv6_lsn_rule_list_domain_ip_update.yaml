
- name: Update a10_cgnv6_lsn_rule_list_domain_ip example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_lsn_rule_list_domain_ip instance
    a10_cgnv6_lsn_rule_list_domain_ip:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
