
- name: Create a10_cgnv6_lsn_radius_profile example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_lsn_radius_profile instance
    a10_cgnv6_lsn_radius_profile:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      lid_profile_index: 1
