- name: Delete a10_access_list_ipv4 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_access_list_ipv4 instance
    a10_access_list_ipv4:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

