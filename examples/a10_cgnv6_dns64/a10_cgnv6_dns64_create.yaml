
- name: Create a10_cgnv6_dns64 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_dns64 instance
    a10_cgnv6_dns64:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
