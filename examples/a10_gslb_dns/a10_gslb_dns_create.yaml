
- name: Create a10_gslb_dns example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_dns instance
    a10_gslb_dns:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
