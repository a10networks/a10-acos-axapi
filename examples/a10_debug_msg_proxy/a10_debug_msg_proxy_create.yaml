
- name: Create a10_debug_msg_proxy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_msg_proxy instance
    a10_debug_msg_proxy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
