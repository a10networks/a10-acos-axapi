- name: Delete a10_fw_vrid example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_fw_vrid instance
    a10_fw_vrid:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


