
- name: Create a10_snmp_server_enable example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_enable instance
    a10_snmp_server_enable:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
