- name: Delete a10_router_ospf example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_router_ospf instance
    a10_router_ospf:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


























