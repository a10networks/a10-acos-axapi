
- name: Create a10_vpn_ipsec_bind_tunnel example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_vpn_ipsec_bind_tunnel instance
    a10_vpn_ipsec_bind_tunnel:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
