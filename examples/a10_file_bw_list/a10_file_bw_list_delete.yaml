- name: Delete a10_file_bw_list example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_bw_list instance
    a10_file_bw_list:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






