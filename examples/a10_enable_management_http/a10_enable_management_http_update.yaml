
- name: Update a10_enable_management_http example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_enable_management_http instance
    a10_enable_management_http:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
