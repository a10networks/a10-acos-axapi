
- name: Update a10_sys_ut_template_l2_mac example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_sys_ut_template_l2_mac instance
    a10_sys_ut_template_l2_mac:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      src_dst: dest
