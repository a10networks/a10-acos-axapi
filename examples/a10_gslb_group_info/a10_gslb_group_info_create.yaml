
- name: Create a10_gslb_group_info example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_group_info instance
    a10_gslb_group_info:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
