
- name: Create a10_admin_ssh_pubkey example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_admin_ssh_pubkey instance
    a10_admin_ssh_pubkey:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
