
- name: Update a10_vpn_errordump example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_vpn_errordump instance
    a10_vpn_errordump:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
