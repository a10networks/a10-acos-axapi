
- name: Update a10_automatic_update_revert example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_automatic_update_revert instance
    a10_automatic_update_revert:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
