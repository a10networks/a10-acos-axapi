
- name: Update a10_file_inspection_global_stat example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_inspection_global_stat instance
    a10_file_inspection_global_stat:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
