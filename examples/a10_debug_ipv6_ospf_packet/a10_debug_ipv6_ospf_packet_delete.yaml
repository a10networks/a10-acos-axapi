- name: Delete a10_debug_ipv6_ospf_packet example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_debug_ipv6_ospf_packet instance
    a10_debug_ipv6_ospf_packet:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent









