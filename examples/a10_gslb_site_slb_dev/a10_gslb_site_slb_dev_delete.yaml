- name: Delete a10_gslb_site_slb_dev example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_site_slb_dev instance
    a10_gslb_site_slb_dev:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



      device_name: NO_EXAMPLE













