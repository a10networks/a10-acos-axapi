
- name: Create a10_cgnv6_nat_icmp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_nat_icmp instance
    a10_cgnv6_nat_icmp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
