
- name: Create a10_cgnv6_lsn_port_overloading_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_lsn_port_overloading_tcp instance
    a10_cgnv6_lsn_port_overloading_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
