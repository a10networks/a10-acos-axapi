
- name: Create a10_router_bgp_neighbor_peer_group_neighbor example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_router_bgp_neighbor_peer_group_neighbor instance
    a10_router_bgp_neighbor_peer_group_neighbor:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      peer_group: NO_EXAMPLE
