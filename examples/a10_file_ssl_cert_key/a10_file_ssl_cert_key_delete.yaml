- name: Delete a10_file_ssl_cert_key example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_ssl_cert_key instance
    a10_file_ssl_cert_key:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent





