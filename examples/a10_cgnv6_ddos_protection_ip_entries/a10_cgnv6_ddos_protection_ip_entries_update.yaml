
- name: Update a10_cgnv6_ddos_protection_ip_entries example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_ddos_protection_ip_entries instance
    a10_cgnv6_ddos_protection_ip_entries:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
