
- name: Create a10_debug_auth example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_auth instance
    a10_debug_auth:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
