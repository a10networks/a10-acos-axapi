
- name: Update a10_cgnv6_fixed_nat_user_quota example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_fixed_nat_user_quota instance
    a10_cgnv6_fixed_nat_user_quota:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
