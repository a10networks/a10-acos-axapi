
- name: Create a10_fw_urpf example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fw_urpf instance
    a10_fw_urpf:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
