
- name: Create a10_debug_vector example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_vector instance
    a10_debug_vector:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
