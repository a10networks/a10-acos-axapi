
- name: Update a10_debug_hw_compression example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_hw_compression instance
    a10_debug_hw_compression:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
