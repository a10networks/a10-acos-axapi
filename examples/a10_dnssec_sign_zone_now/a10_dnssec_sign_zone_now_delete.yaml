- name: Delete a10_dnssec_sign_zone_now example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_dnssec_sign_zone_now instance
    a10_dnssec_sign_zone_now:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

