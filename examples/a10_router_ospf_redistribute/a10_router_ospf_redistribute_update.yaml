
- name: Update a10_router_ospf_redistribute example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_router_ospf_redistribute instance
    a10_router_ospf_redistribute:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
