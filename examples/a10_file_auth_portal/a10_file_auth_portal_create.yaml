
- name: Create a10_file_auth_portal example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_auth_portal instance
    a10_file_auth_portal:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
