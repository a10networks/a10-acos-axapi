- name: Delete a10_scaleout_cluster_cluster_devices_device_id example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_scaleout_cluster_cluster_devices_device_id instance
    a10_scaleout_cluster_cluster_devices_device_id:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

      device_id: 1




