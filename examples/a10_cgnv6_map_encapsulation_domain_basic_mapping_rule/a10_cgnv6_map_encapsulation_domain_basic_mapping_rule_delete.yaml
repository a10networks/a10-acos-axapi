- name: Delete a10_cgnv6_map_encapsulation_domain_basic_mapping_rule example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cgnv6_map_encapsulation_domain_basic_mapping_rule instance
    a10_cgnv6_map_encapsulation_domain_basic_mapping_rule:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






