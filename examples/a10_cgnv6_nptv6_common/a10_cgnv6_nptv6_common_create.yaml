
- name: Create a10_cgnv6_nptv6_common example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_nptv6_common instance
    a10_cgnv6_nptv6_common:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
