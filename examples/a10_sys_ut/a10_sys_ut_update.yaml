
- name: Update a10_sys_ut example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_sys_ut instance
    a10_sys_ut:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
