- name: Delete a10_config_sync_status example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_config_sync_status instance
    a10_config_sync_status:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

