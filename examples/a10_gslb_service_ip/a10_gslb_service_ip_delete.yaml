- name: Delete a10_gslb_service_ip example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_service_ip instance
    a10_gslb_service_ip:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




      node_name: NO_EXAMPLE









