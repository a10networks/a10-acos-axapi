- name: Delete a10_file_policy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_policy instance
    a10_file_policy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






