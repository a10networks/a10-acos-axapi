
- name: Update a10_delete_health_external example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_delete_health_external instance
    a10_delete_health_external:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
