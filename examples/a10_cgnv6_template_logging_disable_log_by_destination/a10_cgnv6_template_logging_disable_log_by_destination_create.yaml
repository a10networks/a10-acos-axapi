
- name: Create a10_cgnv6_template_logging_disable_log_by_destination example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_template_logging_disable_log_by_destination instance
    a10_cgnv6_template_logging_disable_log_by_destination:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
