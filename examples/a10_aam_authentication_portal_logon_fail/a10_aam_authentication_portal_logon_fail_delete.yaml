- name: Delete a10_aam_authentication_portal_logon_fail example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_aam_authentication_portal_logon_fail instance
    a10_aam_authentication_portal_logon_fail:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




