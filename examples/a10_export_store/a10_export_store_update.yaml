
- name: Update a10_export_store example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_export_store instance
    a10_export_store:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
