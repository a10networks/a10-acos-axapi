- name: Delete a10_gslb_ip_list_info example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_ip_list_info instance
    a10_gslb_ip_list_info:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

