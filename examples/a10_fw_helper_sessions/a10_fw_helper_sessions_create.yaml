
- name: Create a10_fw_helper_sessions example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fw_helper_sessions instance
    a10_fw_helper_sessions:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
