- name: Delete a10_aam_file_category_log_top_filename example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_aam_file_category_log_top_filename instance
    a10_aam_file_category_log_top_filename:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

