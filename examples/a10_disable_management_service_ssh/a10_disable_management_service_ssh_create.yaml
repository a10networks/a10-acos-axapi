
- name: Create a10_disable_management_service_ssh example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_disable_management_service_ssh instance
    a10_disable_management_service_ssh:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
