- name: Delete a10_router_ipv6_ospf_default_information example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_router_ipv6_ospf_default_information instance
    a10_router_ipv6_ospf_default_information:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






