- name: Delete a10_automatic_update example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_automatic_update instance
    a10_automatic_update:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent









