
- name: Create a10_cgnv6_ds_lite_port_reservation_entries example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_ds_lite_port_reservation_entries instance
    a10_cgnv6_ds_lite_port_reservation_entries:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
