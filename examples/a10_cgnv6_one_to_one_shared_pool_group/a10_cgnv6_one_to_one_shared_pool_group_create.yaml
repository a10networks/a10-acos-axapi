
- name: Create a10_cgnv6_one_to_one_shared_pool_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_one_to_one_shared_pool_group instance
    a10_cgnv6_one_to_one_shared_pool_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
