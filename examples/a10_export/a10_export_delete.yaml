- name: Delete a10_export example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_export instance
    a10_export:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent








































