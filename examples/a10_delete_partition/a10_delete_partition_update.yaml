
- name: Update a10_delete_partition example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_delete_partition instance
    a10_delete_partition:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
