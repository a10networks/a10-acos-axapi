
- name: Create a10_file_waf example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_waf instance
    a10_file_waf:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
