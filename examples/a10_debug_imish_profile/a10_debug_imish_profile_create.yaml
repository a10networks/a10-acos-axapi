
- name: Create a10_debug_imish_profile example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_imish_profile instance
    a10_debug_imish_profile:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
