
- name: Create a10_router_isis example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_router_isis instance
    a10_router_isis:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      tag: NO_EXAMPLE
