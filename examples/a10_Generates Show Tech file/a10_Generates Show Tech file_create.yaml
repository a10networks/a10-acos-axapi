
- name: Create a10_Generates Show Tech file example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_Generates Show Tech file instance
    a10_Generates Show Tech file:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
