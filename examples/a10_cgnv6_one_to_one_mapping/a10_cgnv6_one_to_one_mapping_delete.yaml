- name: Delete a10_cgnv6_one_to_one_mapping example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cgnv6_one_to_one_mapping instance
    a10_cgnv6_one_to_one_mapping:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

