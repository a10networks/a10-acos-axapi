
- name: Create a10_cgnv6_sixrd_domain example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_sixrd_domain instance
    a10_cgnv6_sixrd_domain:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_domain
