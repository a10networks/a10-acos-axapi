
- name: Create a10_file_syslog example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_syslog instance
    a10_file_syslog:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
