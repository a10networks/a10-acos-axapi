
- name: Create a10_sys_ut_common example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_sys_ut_common instance
    a10_sys_ut_common:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
