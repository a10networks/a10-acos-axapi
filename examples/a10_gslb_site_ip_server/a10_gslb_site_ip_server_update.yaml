
- name: Update a10_gslb_site_ip_server example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_site_ip_server instance
    a10_gslb_site_ip_server:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      ip_server_name: NO_EXAMPLE
