
- name: Create a10_snmp_server_enable_traps_network example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_enable_traps_network instance
    a10_snmp_server_enable_traps_network:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
