
- name: Create a10_enable_management_service_https_acl_v4 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_enable_management_service_https_acl_v4 instance
    a10_enable_management_service_https_acl_v4:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      acl_id: 1
