
- name: Update a10_cgnv6_nat_pool_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_nat_pool_group instance
    a10_cgnv6_nat_pool_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      pool_group_name: NO_EXAMPLE
