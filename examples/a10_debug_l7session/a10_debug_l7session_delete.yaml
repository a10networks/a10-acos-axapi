- name: Delete a10_debug_l7session example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_debug_l7session instance
    a10_debug_l7session:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


