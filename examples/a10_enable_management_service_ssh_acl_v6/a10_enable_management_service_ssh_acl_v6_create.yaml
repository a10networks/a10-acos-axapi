
- name: Create a10_enable_management_service_ssh_acl_v6 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_enable_management_service_ssh_acl_v6 instance
    a10_enable_management_service_ssh_acl_v6:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      acl_name: NO_EXAMPLE
