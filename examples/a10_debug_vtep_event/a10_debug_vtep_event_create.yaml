
- name: Create a10_debug_vtep_event example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_vtep_event instance
    a10_debug_vtep_event:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
