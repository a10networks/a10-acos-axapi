- name: Delete a10_acos_events_message_id example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_acos_events_message_id instance
    a10_acos_events_message_id:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent
      log_msg: NO_EXAMPLE




