
- name: Create a10_sys_ut_template_l2 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_sys_ut_template_l2 instance
    a10_sys_ut_template_l2:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
