
- name: Create a10_boot_block_fix example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_boot_block_fix instance
    a10_boot_block_fix:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
