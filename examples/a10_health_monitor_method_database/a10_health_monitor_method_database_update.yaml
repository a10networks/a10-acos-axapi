
- name: Update a10_health_monitor_method_database example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_health_monitor_method_database instance
    a10_health_monitor_method_database:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
