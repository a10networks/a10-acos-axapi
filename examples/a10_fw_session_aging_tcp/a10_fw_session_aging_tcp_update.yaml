
- name: Update a10_fw_session_aging_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_fw_session_aging_tcp instance
    a10_fw_session_aging_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
