
- name: Update a10_cgnv6_logging_source_address example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_logging_source_address instance
    a10_cgnv6_logging_source_address:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
