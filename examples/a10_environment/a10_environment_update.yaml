
- name: Update a10_environment example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_environment instance
    a10_environment:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
