- name: Delete a10_acos_events_active_template example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_acos_events_active_template instance
    a10_acos_events_active_template:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


