- name: Delete a10_cgnv6_nat_inside_source_statistics example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cgnv6_nat_inside_source_statistics instance
    a10_cgnv6_nat_inside_source_statistics:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

