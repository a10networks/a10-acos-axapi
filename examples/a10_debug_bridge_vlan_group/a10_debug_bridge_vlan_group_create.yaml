
- name: Create a10_debug_bridge_vlan_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_bridge_vlan_group instance
    a10_debug_bridge_vlan_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
