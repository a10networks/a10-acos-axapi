
- name: Create a10_debug_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_tcp instance
    a10_debug_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
