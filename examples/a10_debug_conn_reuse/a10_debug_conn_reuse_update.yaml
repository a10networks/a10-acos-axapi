
- name: Update a10_debug_conn_reuse example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_conn_reuse instance
    a10_debug_conn_reuse:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
