- name: Delete a10_allow_slb_cfg example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_allow_slb_cfg instance
    a10_allow_slb_cfg:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


