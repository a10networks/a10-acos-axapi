
- name: Create a10_snmp_server_enable_traps example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_enable_traps instance
    a10_snmp_server_enable_traps:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
