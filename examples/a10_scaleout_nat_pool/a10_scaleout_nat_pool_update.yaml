
- name: Update a10_scaleout_nat_pool example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_scaleout_nat_pool instance
    a10_scaleout_nat_pool:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
