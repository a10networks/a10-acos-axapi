- name: Delete a10_snmp_server_management_index example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_snmp_server_management_index instance
    a10_snmp_server_management_index:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


