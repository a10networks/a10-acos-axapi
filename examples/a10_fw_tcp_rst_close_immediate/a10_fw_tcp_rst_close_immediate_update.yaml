
- name: Update a10_fw_tcp_rst_close_immediate example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_fw_tcp_rst_close_immediate instance
    a10_fw_tcp_rst_close_immediate:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
