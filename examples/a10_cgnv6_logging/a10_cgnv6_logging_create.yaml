
- name: Create a10_cgnv6_logging example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_logging instance
    a10_cgnv6_logging:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
