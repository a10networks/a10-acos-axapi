
- name: Update a10_authentication example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_authentication instance
    a10_authentication:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
