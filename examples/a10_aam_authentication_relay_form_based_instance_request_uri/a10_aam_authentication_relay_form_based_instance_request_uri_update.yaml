
- name: Update a10_aam_authentication_relay_form_based_instance_request_uri example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_aam_authentication_relay_form_based_instance_request_uri instance
    a10_aam_authentication_relay_form_based_instance_request_uri:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      uri: NO_EXAMPLE
      match_type: equals
