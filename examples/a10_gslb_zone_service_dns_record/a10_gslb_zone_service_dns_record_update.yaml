
- name: Update a10_gslb_zone_service_dns_record example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_zone_service_dns_record instance
    a10_gslb_zone_service_dns_record:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      type: 1
