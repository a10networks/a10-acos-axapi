- name: Delete a10_application_type example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_application_type instance
    a10_application_type:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


