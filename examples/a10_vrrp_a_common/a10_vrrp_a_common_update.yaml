
- name: Update a10_vrrp_a_common example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_vrrp_a_common instance
    a10_vrrp_a_common:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
