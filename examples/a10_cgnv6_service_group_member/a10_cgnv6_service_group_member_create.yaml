
- name: Create a10_cgnv6_service_group_member example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_service_group_member instance
    a10_cgnv6_service_group_member:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_member
