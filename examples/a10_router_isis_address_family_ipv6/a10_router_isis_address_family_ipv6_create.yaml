
- name: Create a10_router_isis_address_family_ipv6 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_router_isis_address_family_ipv6 instance
    a10_router_isis_address_family_ipv6:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
