
- name: Create a10_cloud_services_meta_data example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cloud_services_meta_data instance
    a10_cloud_services_meta_data:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
