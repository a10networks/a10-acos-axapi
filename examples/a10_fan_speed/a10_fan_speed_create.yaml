
- name: Create a10_fan_speed example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fan_speed instance
    a10_fan_speed:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
