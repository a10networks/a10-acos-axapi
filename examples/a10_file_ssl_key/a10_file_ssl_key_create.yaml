
- name: Create a10_file_ssl_key example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_ssl_key instance
    a10_file_ssl_key:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
