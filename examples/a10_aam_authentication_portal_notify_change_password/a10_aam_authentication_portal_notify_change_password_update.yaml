
- name: Update a10_aam_authentication_portal_notify_change_password example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_aam_authentication_portal_notify_change_password instance
    a10_aam_authentication_portal_notify_change_password:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
