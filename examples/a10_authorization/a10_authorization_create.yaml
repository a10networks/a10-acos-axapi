
- name: Create a10_authorization example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_authorization instance
    a10_authorization:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
