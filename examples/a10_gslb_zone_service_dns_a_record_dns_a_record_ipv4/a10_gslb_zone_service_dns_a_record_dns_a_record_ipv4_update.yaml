
- name: Update a10_gslb_zone_service_dns_a_record_dns_a_record_ipv4 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_zone_service_dns_a_record_dns_a_record_ipv4 instance
    a10_gslb_zone_service_dns_a_record_dns_a_record_ipv4:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      dns_a_record_ip: 10.0.0.1
