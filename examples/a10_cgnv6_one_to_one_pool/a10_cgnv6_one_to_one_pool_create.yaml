
- name: Create a10_cgnv6_one_to_one_pool example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_one_to_one_pool instance
    a10_cgnv6_one_to_one_pool:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      pool_name: NO_EXAMPLE
