
- name: Update a10_automatic_update_check_now example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_automatic_update_check_now instance
    a10_automatic_update_check_now:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
