- name: Delete a10_audit example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_audit instance
    a10_audit:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




