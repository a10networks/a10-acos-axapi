
- name: Create a10_dnssec_template example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_dnssec_template instance
    a10_dnssec_template:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      dnssec_temp_name: NO_EXAMPLE
