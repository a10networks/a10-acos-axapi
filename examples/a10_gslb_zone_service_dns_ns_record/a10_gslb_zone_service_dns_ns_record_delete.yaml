- name: Delete a10_gslb_zone_service_dns_ns_record example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_zone_service_dns_ns_record instance
    a10_gslb_zone_service_dns_ns_record:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

      ns_name: NO_EXAMPLE



