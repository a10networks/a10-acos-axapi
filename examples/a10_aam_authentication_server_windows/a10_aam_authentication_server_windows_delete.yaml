- name: Delete a10_aam_authentication_server_windows example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_aam_authentication_server_windows instance
    a10_aam_authentication_server_windows:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



