- name: Delete a10_automatic_update_info example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_automatic_update_info instance
    a10_automatic_update_info:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

