
- name: Update a10_file_glm_license example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_glm_license instance
    a10_file_glm_license:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
