
- name: Create a10_admin_access example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_admin_access instance
    a10_admin_access:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
