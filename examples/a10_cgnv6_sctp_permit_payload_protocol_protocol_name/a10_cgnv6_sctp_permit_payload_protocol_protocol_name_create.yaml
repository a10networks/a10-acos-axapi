
- name: Create a10_cgnv6_sctp_permit_payload_protocol_protocol_name example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_sctp_permit_payload_protocol_protocol_name instance
    a10_cgnv6_sctp_permit_payload_protocol_protocol_name:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      protocol: iua
