- name: Delete a10_cgnv6_lsn_endpoint_independent_filtering_udp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cgnv6_lsn_endpoint_independent_filtering_udp instance
    a10_cgnv6_lsn_endpoint_independent_filtering_udp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



