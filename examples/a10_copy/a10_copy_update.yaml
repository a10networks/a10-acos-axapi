
- name: Update a10_copy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_copy instance
    a10_copy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
