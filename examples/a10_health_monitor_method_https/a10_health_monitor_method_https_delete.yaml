- name: Delete a10_health_monitor_method_https example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_health_monitor_method_https instance
    a10_health_monitor_method_https:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






























