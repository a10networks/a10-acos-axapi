
- name: Update a10_health_monitor_method_tacplus example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_health_monitor_method_tacplus instance
    a10_health_monitor_method_tacplus:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
