
- name: Update a10_cgnv6_nptv6_domain example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_nptv6_domain instance
    a10_cgnv6_nptv6_domain:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      name: my_domain
