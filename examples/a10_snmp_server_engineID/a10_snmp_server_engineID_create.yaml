
- name: Create a10_snmp_server_engineID example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_engineID instance
    a10_snmp_server_engineID:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
