
- name: Update a10_fw_app example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_fw_app instance
    a10_fw_app:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
