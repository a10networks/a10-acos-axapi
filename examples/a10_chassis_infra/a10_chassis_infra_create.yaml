
- name: Create a10_chassis_infra example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_chassis_infra instance
    a10_chassis_infra:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
