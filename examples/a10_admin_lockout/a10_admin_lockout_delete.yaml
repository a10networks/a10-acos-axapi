- name: Delete a10_admin_lockout example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_admin_lockout instance
    a10_admin_lockout:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent





