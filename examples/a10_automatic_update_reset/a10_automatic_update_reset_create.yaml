
- name: Create a10_automatic_update_reset example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_automatic_update_reset instance
    a10_automatic_update_reset:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
