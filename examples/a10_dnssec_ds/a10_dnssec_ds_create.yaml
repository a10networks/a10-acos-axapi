
- name: Create a10_dnssec_ds example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_dnssec_ds instance
    a10_dnssec_ds:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
