
- name: Create a10_gslb_zone example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_zone instance
    a10_gslb_zone:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      name: my_zone
