
- name: Create a10_cgnv6_ddos_protection_disable_nat_ip_by_bgp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_ddos_protection_disable_nat_ip_by_bgp instance
    a10_cgnv6_ddos_protection_disable_nat_ip_by_bgp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
