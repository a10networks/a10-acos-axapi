
- name: Create a10_file_aflex example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_aflex instance
    a10_file_aflex:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
