
- name: Update a10_fw_active_rule_set example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_fw_active_rule_set instance
    a10_fw_active_rule_set:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
