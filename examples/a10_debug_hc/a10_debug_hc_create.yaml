
- name: Create a10_debug_hc example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_hc instance
    a10_debug_hc:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
