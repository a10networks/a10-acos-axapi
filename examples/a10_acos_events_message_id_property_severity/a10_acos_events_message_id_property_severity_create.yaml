
- name: Create a10_acos_events_message_id_property_severity example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_acos_events_message_id_property_severity instance
    a10_acos_events_message_id_property_severity:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
