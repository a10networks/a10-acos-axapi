
- name: Update a10_debug_bgp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_bgp instance
    a10_debug_bgp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
