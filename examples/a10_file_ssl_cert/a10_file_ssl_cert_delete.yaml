- name: Delete a10_file_ssl_cert example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_ssl_cert instance
    a10_file_ssl_cert:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent








