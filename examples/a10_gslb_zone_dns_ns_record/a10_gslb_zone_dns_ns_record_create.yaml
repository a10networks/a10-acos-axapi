
- name: Create a10_gslb_zone_dns_ns_record example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_zone_dns_ns_record instance
    a10_gslb_zone_dns_ns_record:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      ns_name: NO_EXAMPLE
