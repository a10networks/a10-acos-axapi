
- name: Create a10_snmp_server_enable_traps_vrrp_a example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_enable_traps_vrrp_a instance
    a10_snmp_server_enable_traps_vrrp_a:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
