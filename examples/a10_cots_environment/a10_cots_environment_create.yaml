
- name: Create a10_cots_environment example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cots_environment instance
    a10_cots_environment:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
