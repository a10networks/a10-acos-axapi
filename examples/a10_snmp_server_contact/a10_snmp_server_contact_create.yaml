
- name: Create a10_snmp_server_contact example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_snmp_server_contact instance
    a10_snmp_server_contact:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
