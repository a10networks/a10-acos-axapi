
- name: Create a10_debug_lacp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_lacp instance
    a10_debug_lacp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
