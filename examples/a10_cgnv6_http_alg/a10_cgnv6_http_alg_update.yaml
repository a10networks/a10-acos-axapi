
- name: Update a10_cgnv6_http_alg example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_http_alg instance
    a10_cgnv6_http_alg:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
