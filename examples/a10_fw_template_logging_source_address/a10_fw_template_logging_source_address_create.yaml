
- name: Create a10_fw_template_logging_source_address example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fw_template_logging_source_address instance
    a10_fw_template_logging_source_address:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
