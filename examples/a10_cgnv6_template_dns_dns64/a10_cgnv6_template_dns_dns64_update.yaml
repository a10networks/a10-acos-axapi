
- name: Update a10_cgnv6_template_dns_dns64 example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_template_dns_dns64 instance
    a10_cgnv6_template_dns_dns64:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
