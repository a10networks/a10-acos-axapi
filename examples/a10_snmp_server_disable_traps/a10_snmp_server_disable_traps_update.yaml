
- name: Update a10_snmp_server_disable_traps example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_snmp_server_disable_traps instance
    a10_snmp_server_disable_traps:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
