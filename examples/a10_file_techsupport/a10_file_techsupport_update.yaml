
- name: Update a10_file_techsupport example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_file_techsupport instance
    a10_file_techsupport:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
