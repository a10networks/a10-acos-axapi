- name: Delete a10_file_dnssec_dnskey example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_dnssec_dnskey instance
    a10_file_dnssec_dnskey:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






