
- name: Create a10_vpn_error example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_vpn_error instance
    a10_vpn_error:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
