
- name: Update a10_cgnv6_logging_nat_quota_exceeded example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_logging_nat_quota_exceeded instance
    a10_cgnv6_logging_nat_quota_exceeded:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
