
- name: Update a10_cgnv6_nat64_alg_tftp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_nat64_alg_tftp instance
    a10_cgnv6_nat64_alg_tftp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
