
- name: Update a10_gslb_zone_service_dns_a_record_dns_a_record_srv example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_zone_service_dns_a_record_dns_a_record_srv instance
    a10_gslb_zone_service_dns_a_record_dns_a_record_srv:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      svrname: NO_EXAMPLE
