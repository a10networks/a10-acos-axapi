
- name: Update a10_gslb_system example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_system instance
    a10_gslb_system:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
