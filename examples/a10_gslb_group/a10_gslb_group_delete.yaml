- name: Delete a10_gslb_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_gslb_group instance
    a10_gslb_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

















      name: my_group

