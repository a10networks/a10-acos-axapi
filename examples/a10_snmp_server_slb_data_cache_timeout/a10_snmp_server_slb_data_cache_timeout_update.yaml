
- name: Update a10_snmp_server_slb_data_cache_timeout example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_snmp_server_slb_data_cache_timeout instance
    a10_snmp_server_slb_data_cache_timeout:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
