
- name: Create a10_aam_file_category_log_top_destip example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_aam_file_category_log_top_destip instance
    a10_aam_file_category_log_top_destip:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
