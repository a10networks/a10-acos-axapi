
- name: Create a10_enable_core example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_enable_core instance
    a10_enable_core:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
