
- name: Create a10_file_ssl_crl example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_ssl_crl instance
    a10_file_ssl_crl:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
