- name: Delete a10_snmp_server_location example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_snmp_server_location instance
    a10_snmp_server_location:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


