
- name: Update a10_sys_ut_event_action_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_sys_ut_event_action_tcp instance
    a10_sys_ut_event_action_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
