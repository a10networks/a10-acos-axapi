- name: Delete a10_backup_system example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_backup_system instance
    a10_backup_system:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




