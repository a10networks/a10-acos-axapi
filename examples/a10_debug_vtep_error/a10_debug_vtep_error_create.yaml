
- name: Create a10_debug_vtep_error example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_vtep_error instance
    a10_debug_vtep_error:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
