
- name: Update a10_enable_management_service_https example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_enable_management_service_https instance
    a10_enable_management_service_https:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
