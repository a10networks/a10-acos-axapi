
- name: Update a10_aam_authentication_server_radius_instance example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_aam_authentication_server_radius_instance instance
    a10_aam_authentication_server_radius_instance:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
      name: my_instance
