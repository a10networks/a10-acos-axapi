
- name: Create a10_sys_ut_stop_test example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_sys_ut_stop_test instance
    a10_sys_ut_stop_test:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
