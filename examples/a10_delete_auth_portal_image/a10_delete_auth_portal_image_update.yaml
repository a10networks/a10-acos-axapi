
- name: Update a10_delete_auth_portal_image example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_delete_auth_portal_image instance
    a10_delete_auth_portal_image:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
