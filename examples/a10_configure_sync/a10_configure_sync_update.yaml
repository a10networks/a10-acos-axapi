
- name: Update a10_configure_sync example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_configure_sync instance
    a10_configure_sync:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
