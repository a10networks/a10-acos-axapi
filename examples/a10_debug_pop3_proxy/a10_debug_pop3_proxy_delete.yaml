- name: Delete a10_debug_pop3_proxy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_debug_pop3_proxy instance
    a10_debug_pop3_proxy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

