
- name: Create a10_axdebug_capture example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_axdebug_capture instance
    a10_axdebug_capture:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
