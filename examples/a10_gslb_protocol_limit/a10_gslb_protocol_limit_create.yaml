
- name: Create a10_gslb_protocol_limit example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_protocol_limit instance
    a10_gslb_protocol_limit:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
