- name: Delete a10_health_monitor_method_external example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_health_monitor_method_external instance
    a10_health_monitor_method_external:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent






