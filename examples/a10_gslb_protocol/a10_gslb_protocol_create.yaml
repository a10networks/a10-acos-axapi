
- name: Create a10_gslb_protocol example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_protocol instance
    a10_gslb_protocol:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
