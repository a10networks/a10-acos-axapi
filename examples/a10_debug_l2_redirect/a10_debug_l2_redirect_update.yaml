
- name: Update a10_debug_l2_redirect example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_l2_redirect instance
    a10_debug_l2_redirect:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
