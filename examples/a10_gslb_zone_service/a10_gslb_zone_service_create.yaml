
- name: Create a10_gslb_zone_service example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_gslb_zone_service instance
    a10_gslb_zone_service:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      service_name: NO_EXAMPLE
