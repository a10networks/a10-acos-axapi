
- name: Create a10_aam_authentication_server_ldap example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_aam_authentication_server_ldap instance
    a10_aam_authentication_server_ldap:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
