
- name: Update a10_vrrp_a_force_self_standby example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_vrrp_a_force_self_standby instance
    a10_vrrp_a_force_self_standby:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
