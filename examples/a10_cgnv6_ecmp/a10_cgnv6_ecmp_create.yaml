
- name: Create a10_cgnv6_ecmp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_cgnv6_ecmp instance
    a10_cgnv6_ecmp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
