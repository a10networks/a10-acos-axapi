
- name: Create a10_aam_file_category_log_count_in_interval example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_aam_file_category_log_count_in_interval instance
    a10_aam_file_category_log_count_in_interval:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
