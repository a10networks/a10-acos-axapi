
- name: Update a10_vrrp_a_peer_group example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_vrrp_a_peer_group instance
    a10_vrrp_a_peer_group:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
