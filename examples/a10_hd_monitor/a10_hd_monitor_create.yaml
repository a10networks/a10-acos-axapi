
- name: Create a10_hd_monitor example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_hd_monitor instance
    a10_hd_monitor:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
