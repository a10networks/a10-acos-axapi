- name: Delete a10_cgnv6_nat64_full_cone_session example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_cgnv6_nat64_full_cone_session instance
    a10_cgnv6_nat64_full_cone_session:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

