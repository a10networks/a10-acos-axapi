
- name: Update a10_debug_es example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_es instance
    a10_debug_es:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
