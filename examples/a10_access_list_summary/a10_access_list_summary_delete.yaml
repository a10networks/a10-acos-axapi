- name: Delete a10_access_list_summary example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_access_list_summary instance
    a10_access_list_summary:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent

