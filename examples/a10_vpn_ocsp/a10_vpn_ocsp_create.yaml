
- name: Create a10_vpn_ocsp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_vpn_ocsp instance
    a10_vpn_ocsp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
