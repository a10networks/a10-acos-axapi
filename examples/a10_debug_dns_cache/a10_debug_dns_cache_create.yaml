
- name: Create a10_debug_dns_cache example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_dns_cache instance
    a10_debug_dns_cache:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
