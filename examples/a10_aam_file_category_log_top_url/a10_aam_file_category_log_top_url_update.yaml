
- name: Update a10_aam_file_category_log_top_url example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_aam_file_category_log_top_url instance
    a10_aam_file_category_log_top_url:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
