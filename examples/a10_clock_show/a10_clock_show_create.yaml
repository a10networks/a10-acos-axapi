
- name: Create a10_clock_show example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_clock_show instance
    a10_clock_show:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
