
- name: Create a10_file_glm_cert example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_file_glm_cert instance
    a10_file_glm_cert:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
