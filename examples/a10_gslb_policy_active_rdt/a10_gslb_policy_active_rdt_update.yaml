
- name: Update a10_gslb_policy_active_rdt example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_gslb_policy_active_rdt instance
    a10_gslb_policy_active_rdt:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
