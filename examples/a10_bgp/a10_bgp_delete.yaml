- name: Delete a10_bgp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_bgp instance
    a10_bgp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



