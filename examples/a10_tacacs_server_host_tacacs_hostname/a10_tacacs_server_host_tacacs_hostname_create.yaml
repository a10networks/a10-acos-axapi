
- name: Create a10_tacacs_server_host_tacacs_hostname example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_tacacs_server_host_tacacs_hostname instance
    a10_tacacs_server_host_tacacs_hostname:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      hostname: NO_EXAMPLE
