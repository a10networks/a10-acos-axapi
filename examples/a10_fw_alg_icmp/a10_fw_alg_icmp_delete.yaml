- name: Delete a10_fw_alg_icmp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_fw_alg_icmp instance
    a10_fw_alg_icmp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


