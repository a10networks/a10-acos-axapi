
- name: Update a10_debug_management example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_debug_management instance
    a10_debug_management:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
