
- name: Create a10_sys_ut_template_ignore_validation example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_sys_ut_template_ignore_validation instance
    a10_sys_ut_template_ignore_validation:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
