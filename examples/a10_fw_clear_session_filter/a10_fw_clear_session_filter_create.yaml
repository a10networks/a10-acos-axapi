
- name: Create a10_fw_clear_session_filter example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fw_clear_session_filter instance
    a10_fw_clear_session_filter:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
