
- name: Create a10_delete_startup_config example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_delete_startup_config instance
    a10_delete_startup_config:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
