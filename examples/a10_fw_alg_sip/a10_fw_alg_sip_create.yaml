
- name: Create a10_fw_alg_sip example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_fw_alg_sip instance
    a10_fw_alg_sip:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
