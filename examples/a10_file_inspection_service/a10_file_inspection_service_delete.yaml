- name: Delete a10_file_inspection_service example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_file_inspection_service instance
    a10_file_inspection_service:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent




