- name: Delete a10_aam_authorization_policy example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_aam_authorization_policy instance
    a10_aam_authorization_policy:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent
      name: my_policy









