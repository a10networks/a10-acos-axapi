
- name: Create a10_debug_migration example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Create a10_debug_migration instance
    a10_debug_migration:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
