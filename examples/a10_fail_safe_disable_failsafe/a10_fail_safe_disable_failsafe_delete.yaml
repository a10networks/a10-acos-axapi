- name: Delete a10_fail_safe_disable_failsafe example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_fail_safe_disable_failsafe instance
    a10_fail_safe_disable_failsafe:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent


