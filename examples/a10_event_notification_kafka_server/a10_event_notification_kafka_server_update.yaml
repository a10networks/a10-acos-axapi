
- name: Update a10_event_notification_kafka_server example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_event_notification_kafka_server instance
    a10_event_notification_kafka_server:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
