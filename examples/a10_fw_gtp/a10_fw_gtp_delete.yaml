- name: Delete a10_fw_gtp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Delete a10_fw_gtp instance
    a10_fw_gtp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: absent



