
- name: Update a10_cgnv6_lsn_endpoint_independent_mapping_tcp example playbook
  connection: local
  hosts: localhost
  tasks:
  - name: Update a10_cgnv6_lsn_endpoint_independent_mapping_tcp instance
    a10_cgnv6_lsn_endpoint_independent_mapping_tcp:
      a10_host: "{{ a10_host }}"
      a10_username: "{{ a10_username }}"
      a10_password: "{{ a10_password }}"
      state: present
